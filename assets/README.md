# Assets

These image files are used by `game.js` with relative paths so the game works on GitHub Pages:

- `player.jpg`
- `enemy.jpg`
- `miniboss.jpg`
- `midboss.jpg`
- `bigboss.jpg`

Keep the file names and letter case exactly the same.
